package sqlpermissions.main;

import sqlpermissions.objects.PermissionHandler;

public class Main {
	
	/*
	 * Not added yet. This class is the plugin's main thread, where
	 * event assignment will occur and passed through to the CommandParser.
	 * 
	 * We will not be using Bukkit/Spigot's internal command handler,
	 * as this will allow commands to be added by the user of the plugin,
	 * via the Framework that will later be designed.
	 */
	
}
